import { StrictMode } from "react"
import { NavBarPage } from "./index"

export const Header = ()=>{
    return(
        <StrictMode>
            <NavBarPage />            
        </StrictMode>
    )
}